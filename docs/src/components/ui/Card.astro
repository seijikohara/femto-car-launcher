---
import { withBase } from "@/lib/url";
import { cn } from "cn";

export interface Props {
    href?: string;
    as?: "div" | "article" | "li";
    padding?: "md" | "lg";
    class?: string;
}

const { href, as = "div", padding = "lg", class: className = "" } = Astro.props;

const Tag = href ? "a" : as;
const classes = cn(
    "block rounded-xl border border-border bg-card text-card-foreground shadow-sm",
    padding === "lg" ? "p-6 sm:p-8" : "p-4 sm:p-6",
    href &&
        "transition-colors hover:border-primary/60 hover:bg-accent/40 focus-visible:outline-2 focus-visible:outline-ring",
    className,
);
---

<Tag class={classes} href={href ? withBase(href) : undefined}>
    <slot />
</Tag>
