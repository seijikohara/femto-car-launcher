package io.github.seijikohara.femto.data.display

/** Google Maps base map type the user can select for the Google Maps backend. */
internal enum class GoogleMapType { ROADMAP, SATELLITE, HYBRID, TERRAIN }
