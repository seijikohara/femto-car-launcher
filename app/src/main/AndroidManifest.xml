<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        ACCESS_COARSE_LOCATION: paired with ACCESS_FINE_LOCATION per
        Android 12+ runtime model — users may grant only coarse;
        the dashboard renders with degraded precision in that case.
        Justification mirrored in CLAUDE.md#permissions.
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!--
        ACCESS_FINE_LOCATION: powers the head-unit dashboard's
        location-driven surfaces — map centring, speed / altitude /
        address overlays, and current-location weather lookup.
        Justification is mirrored in the audit log at
        CLAUDE.md#permissions.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!--
        Declare location hardware as optional so Play does not implicitly
        hard-require GPS / network-location hardware (requesting the
        location permissions above otherwise implies required=true). The
        location-driven panels degrade gracefully — they emit null until
        a fix is available — so a head unit or region without this
        hardware must still see and install the launcher (multi-region
        distribution).
    -->
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.location.network" android:required="false" />

    <!--
        ACCESS_NETWORK_STATE: MapLibre connectivity probe before
        OpenFreeMap vector-tile fetch. Justification mirrored in
        CLAUDE.md#permissions.
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
        ACCESS_WIFI_STATE: read Wi-Fi transport / validation state so
        the footer status cluster reports a live Wi-Fi indicator.
        Normal protection; auto-granted at install.
        Justification mirrored in CLAUDE.md#permissions.
    -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!--
        BLUETOOTH_CONNECT: read the set of currently-connected
        Bluetooth devices (HEADSET / A2DP / GATT) so the footer status
        cluster reflects whether the head unit is paired to a phone or
        an audio device. Dangerous on Android 12+; runtime grant.
        Without it the BT indicator stays dimmed but the rest of the
        launcher remains functional. Justification mirrored in
        CLAUDE.md#permissions.
    -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!--
        INTERNET: Open-Meteo weather API + OpenFreeMap vector-tile
        fetch (MapLibre) + Nominatim/OSM reverse geocoding.
        Justification mirrored in CLAUDE.md#permissions.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        READ_CALENDAR: query CalendarContract.Instances for the
        Calendar card's 6-day strip and upcoming event list.
        Dangerous; runtime grant required. Without it the card
        still shows today's date and an empty event list.
        Justification mirrored in CLAUDE.md#permissions.
    -->
    <uses-permission android:name="android.permission.READ_CALENDAR" />

    <!--
        READ_PHONE_STATE: read the cellular SignalStrength.level via
        TelephonyCallback so the footer status cluster shows graduated
        cellular signal bars. Dangerous; runtime grant. Without it the
        cellular indicator degrades to the binary connected/disconnected
        icon. Justification mirrored in CLAUDE.md#permissions.
    -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!--
        Declare telephony hardware as optional so Play does not implicitly
        hard-require it (requesting READ_PHONE_STATE above otherwise implies
        required=true). Wi-Fi-only AI boxes have no telephony
        hardware; the cellular indicator degrades — cellularFlow()
        and cellularLevelFlow() guard FEATURE_TELEPHONY and emit null — so
        such units must still see and install the launcher (multi-region
        distribution). See CLAUDE.md#launcher-behavior.
    -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <!--
        Package visibility: declare interest in every app that registers
        the LAUNCHER intent so LauncherApps.getActivityList() returns the
        full set on Android 11+. This is the privacy-respecting path
        recommended by the add-launcher-permission skill (the alternative,
        QUERY_ALL_PACKAGES, requires a Play Store justification).
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FemtoCarLauncher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".data.MusicSessionListenerService"
            android:label="@string/music_listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>

</manifest>
