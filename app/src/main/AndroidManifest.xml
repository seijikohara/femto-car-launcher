<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        Per-permission justifications and denied-state degradation live
        in the audit log at .claude/rules/permissions.md (the SSOT,
        kept in sync by the add-launcher-permission skill). Comments
        below cover only manifest mechanics (e.g. optional hardware).
    -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!--
        Declare location hardware as optional so Play does not implicitly
        hard-require GPS / network-location hardware (requesting the
        location permissions above otherwise implies required=true). The
        location-driven panels degrade gracefully — they emit null until
        a fix is available — so a head unit or region without this
        hardware must still see and install the launcher (multi-region
        distribution).
    -->
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.location.network" android:required="false" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
<uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_CALENDAR" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!--
        Declare telephony hardware as optional so Play does not implicitly
        hard-require it (requesting READ_PHONE_STATE above otherwise implies
        required=true). Wi-Fi-only AI boxes have no telephony
        hardware; the cellular indicator degrades — cellularFlow()
        and cellularLevelFlow() guard FEATURE_TELEPHONY and emit null — so
        such units must still see and install the launcher (multi-region
        distribution). See CLAUDE.md#launcher-behavior.
    -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!--
        Declare microphone hardware as optional so Play does not implicitly
        hard-require it (requesting RECORD_AUDIO above otherwise implies
        required=true). Head units without a mic must still see and install
        the launcher (multi-region distribution); the voice surface degrades
        to delegation there.
    -->
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!--
        Package visibility: declare interest in every app that registers
        the LAUNCHER intent so LauncherApps.getActivityList() returns the
        full set on Android 11+. This is the privacy-respecting path
        recommended by the add-launcher-permission skill (the alternative,
        QUERY_ALL_PACKAGES, requires a Play Store justification).
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <!--
            Resolve the device's default assistant so the dock mic can hand
            off to its overlay directly (and fall back to the in-launcher
            voice sheet when nothing resolves).
        -->
        <intent>
            <action android:name="android.intent.action.ASSIST" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FemtoCarLauncher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".data.music.MusicSessionListenerService"
            android:label="@string/music_listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <service
            android:name=".TripTrackingService"
            android:exported="false"
            android:foregroundServiceType="location" />
    </application>

</manifest>
