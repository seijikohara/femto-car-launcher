<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        ACCESS_FINE_LOCATION: powers the head-unit dashboard's
        location-driven surfaces — map centring, speed / altitude /
        address overlays, and current-location weather lookup.
        Justification is mirrored in the audit log at
        CLAUDE.md#permissions.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!--
        ACCESS_NETWORK_STATE: Maps SDK best-practice probe before
        tile fetch. Justification mirrored in CLAUDE.md#permissions.
    -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
        INTERNET: Open-Meteo weather API + Google Maps Lite tile
        fetch. Justification mirrored in CLAUDE.md#permissions.
    -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
        Package visibility: declare interest in every app that registers
        the LAUNCHER intent so LauncherApps.getActivityList() returns the
        full set on Android 11+. This is the privacy-respecting path
        recommended by the add-launcher-permission skill (the alternative,
        QUERY_ALL_PACKAGES, requires a Play Store justification).
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FemtoCarLauncher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".data.MusicSessionListenerService"
            android:label="@string/music_listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />
    </application>

</manifest>
