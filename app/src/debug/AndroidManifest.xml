<?xml version="1.0" encoding="utf-8"?>
<!-- Debug-only: registers the WebView map diagnosis activity.
     Merged into debug builds only; never ships in release. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <activity
            android:name=".MapLibreWebViewActivity"
            android:exported="true" />
    </application>
</manifest>
