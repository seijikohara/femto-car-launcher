*.iml
.gradle
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/deploymentTargetSelector.xml
/.idea/assetWizardSettings.xml
/.idea/.gitignore
/.idea/appInsightsSettings.xml
/.idea/markdown.xml
/.idea/vcs.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties

# Claude Code per-machine settings (committed counterpart is .claude/settings.json)
.claude/settings.local.json

# Superpowers brainstorm scratch (mockups, server state)
/.superpowers/

# Kotlin 2.x incremental compile / daemon cache (regenerated by Gradle)
/.kotlin/

# Playwright MCP run artifacts (screenshots, traces)
/.playwright-mcp/

# Signing keystores and secret material — never commit (rotate if leaked)
*.jks
*.keystore
*.p12
*.b64
keystore.properties
# CocoIndex Code (ccc)
/.cocoindex_code/

# Web map payload (webmap/) — installed deps and the Vite build output that
# Gradle wires into assets; only the TypeScript sources are committed
/webmap/node_modules/
/webmap/dist/
